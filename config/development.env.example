# Copy to .env.launch-staging.local only if that private file does not exist.
# Credentials must belong to the isolated staging project.
NEXT_PUBLIC_SUPABASE_URL=https://cktvxmclcxtymkozciaw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

LAUNCH_POWERSYNC_ENABLED=true
LAUNCH_OFFLINE_MEDIA_ENABLED=true
LAUNCH_BILLING_ENABLED=false

# Optional staging-only integrations. No OpenAI key means mock card generation.
OPENAI_API_KEY=
MISTRAL_API_KEY=
# Use the same staging secret for the local API and extraction worker.
EXTRACTION_WORKER_SECRET=
